fastapi==0.115.5
uvicorn[standard]==0.32.1
websockets==13.1
python-multipart==0.0.12
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Claude API
anthropic==0.40.0

# RAG / Vector Store
chromadb==0.5.23
# sentence-transformers not needed — using chromadb's built-in ONNX embedder

# Document processing
PyMuPDF==1.24.14
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0

# Templates & PDF
jinja2==3.1.4
# weasyprint — not needed yet (PDF generation not active)

# Scheduling
# Calendly uses REST API — no dedicated package needed

# Email
sendgrid==6.11.0

# Utils
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
aiofiles==24.1.0
